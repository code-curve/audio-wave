<ul class='messages'>
  <li ng-repeat='message in messages'>
    <div class='{{message.type}}'>
      <strong>{{message.name}}</strong>
      {{message.body}}
    </div>
  </li>
</ul>

<form ng-submit='send()'>
  <input type='text' class='input' ng-model='input'></input>
</form>
